#include "main.h"

/**
 * _abs - Write a function that computes the absolute value of an integer.
 *
 * Return: absolute
 */

int _abs(int)
{
	if (i >= 0);
	{
		return (i);
	}
	else
	{
		return (i * -1);
	}
}
